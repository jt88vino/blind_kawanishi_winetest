'use client';
import {useState,type FormEvent} from 'react';
export default function Login(){const [password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const d=await r.json();if(!r.ok)throw Error(d.error);location.reload();}catch(e:any){setError(e.message)}finally{setBusy(false)}}return <main className="shell" style={{maxWidth:520}}><p className="eyebrow">BLIND KAWANISHI 2026</p><h1>管理者ログイン</h1><p className="lead">問題の編集・集計結果の確認</p><form onSubmit={submit} className="submitpanel" style={{position:'static'}}><label className="field"><span>管理用パスワード</span><input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required style={{border:'1px solid #ccc',borderRadius:6,padding:12}}/></label>{error&&<p role="alert" className="message error">{error}</p>}<button className="primary" disabled={busy} style={{marginTop:20}}>{busy?'確認中…':'ログイン'}</button></form><p><a href="/">回答ページに戻る</a></p></main>}
